{
  "name": "pipercalc",
  "version": "1.0.0",
  "description": "Calculadora web responsiva com integração de pipelines",
  "main": "index.js",
"scripts": {
  "lint": "npx eslint src/js --ext .js",
  "lint:fix": "npx eslint src/js --ext .js --fix",
  "format": "prettier --write src/**/*.{html,css,js}",
  "test": "jest",
  "start": "live-server src",
  "build": "echo 'Nenhum processo de build configurado ainda'"
},
  "keywords": ["calculadora", "web", "responsiva", "pipelines"],
  "author": "Carrah",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "jest": "^29.0.0"
  }
}